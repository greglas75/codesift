[
  {
    "taskId": "R3",
    "method": "standard",
    "repo": "tgm-survey",
    "durationMs": 96675,
    "inputTokens": 1786,
    "outputTokens": 3295,
    "totalTokens": 5081,
    "cacheReadTokens": 474355,
    "cacheCreationTokens": 55207,
    "costUsd": 0.40411575,
    "numTurns": 16,
    "answer": "I now have a complete picture. Here is the full analysis:\n\n---\n\n## Authentication System Analysis — TGM Survey Platform\n\n### 1. Auth Provider\n\n**Clerk** is the sole auth provider. Legacy JWT auth has been removed on branch `feat/clerk-only-auth`.\n\n- Clerk instance: `dashing-bass-97.clerk.accounts.dev`\n- Frontend SDK: `@clerk/clerk-react` (`ClerkProvider`, `useUser`, `useSession`, `SignIn`)\n- Backend SDK: `@clerk/express` (`clerkMiddleware`)\n- Published key: `VITE_CLERK_PUBLISHABLE_KEY` (frontend env), `CLERK_SECRET_KEY` + `CLERK_PUBLISHABLE_KEY` (backend env)\n\n**No Auth0, no custom JWT issuer, no Passport strategies** remain. `JWT_SECRET` still exists only for preview/share token signing (not user auth).\n\n---\n\n### 2. Login Flow (step by step)\n\n**Frontend entry** — `apps/designer/src/auth/ClerkAuthProvider.tsx:78`\n\n1. `ClerkProvider` wraps the app with `publishableKey` from `env.VITE_CLERK_PUBLISHABLE_KEY`.\n2. Unauthenticated users hit the `ClerkLoginPage` (`ClerkAuthProvider.tsx:107`) which renders Clerk's hosted `<SignIn routing=\"path\" path=\"/login\" />` component — Clerk handles the login UI and credential verification entirely.\n3. On successful sign-in, Clerk sets a session in its hosted infrastructure. The frontend's `useSession()` hook receives a live `session` object.\n4. `ClerkAuthBridge` (inner component, line 20) calls `setClerkTokenGetter(() => session.getToken())` via `useEffect` — this injects the token getter into the API client so every API/tRPC call includes `Authorization: Bearer <clerk-jwt>`.\n5. User identity is read from `clerkUser.publicMetadata` (fields: `dbUserId`, `dbOrgId`, `role`) — no `/api/auth/me` round-trip needed for the frontend.\n\n**Backend validation** — `apps/api/src/auth/clerk.guard.ts:32`\n\n6. Every request hits `ClerkAuthGuard.canActivate()` (registered as global `APP_GUARD`).\n7. **Strategy 1 (production):** `clerkMiddleware` (registered in `main.ts:113`) runs first, setting `req.auth.userId` to the verified Clerk user ID. The guard reads `req.auth?.userId`.\n8. **Strategy 2 (dev mode):** `clerkMiddleware` is skipped in dev. The guard base64-decodes the Bearer JWT manually (`clerk.guard.ts:53-60`), extracts `payload.sub`, and validates it starts with `user_`.\n9. The guard then does a DB lookup: `prisma.user.findUnique({ where: { clerkId: clerkUserId } })` (line 71).\n10. If the user exists and `isActive`, it writes a normalized `req.user = { userId, orgId, clerkUserId, email, role, name }` (line 85).\n11. `req.user` is also the source for the tRPC context (`apps/api/src/trpc/context.ts:46` — same two-strategy extraction + DB lookup).\n\n---\n\n### 3. Guard Chain\n\nRegistered in `apps/api/src/app.module.ts:92-116` as NestJS `APP_GUARD` providers (execute in order):\n\n| Order | Guard | File | Role |\n|-------|-------|------|------|\n| 1 | `ClerkAuthGuard` | `auth/clerk.guard.ts` | Auth: validates Clerk token, populates `req.user`. Allows `@Public()` routes. |\n| 2 | `RolesGuard` | `common/guards/roles.guard.ts` | RBAC: checks `@Roles(...)` decorator against `req.user.role`. `super_admin` bypasses all. |\n| 3 | `FeatureGateGuard` | `modules/feature-gate/feature-gate.guard.ts` | Feature flags: blocks access to feature-gated endpoints by org plan. |\n| 4 | `ThrottlerGuard` | NestJS Throttler | Rate-limiting: 60 req/min (prod), 300 req/min (dev). |\n\nRoutes opt out of auth via `@Public()` decorator (`common/decorators/public.decorator.ts`) — the runner (respondent-facing) is entirely public.\n\n---\n\n### 4. Session / Token State Storage\n\n| Layer | Storage | Details |\n|-------|---------|---------|\n| Frontend session | **Clerk's hosted infrastructure** | Clerk manages session cookies/tokens — not stored in our app. `useSession()` provides a live session object. |\n| API token | **Request-scoped only** | The decoded `req.user` object lives only for the duration of the HTTP request. No Redis/DB session store. |\n| User identity sync | **PostgreSQL** (`User.clerkId` column) | On first login, Clerk fires a `user.created` webhook (`auth/webhook/clerk-webhook.service.ts:41`) which upserts a `User` row with `clerkId`. Guards look up users by this column. |\n| Preview/share tokens | **JWT + PostgreSQL** | `JWT_SECRET` is used only for survey preview/share link tokens, not user auth. |\n\nThere is **no server-side session store** (no Redis sessions, no express-session). Authentication is stateless from the API's perspective — each request is independently validated via the Clerk token.\n\n---\n\n### 5. Is Clerk Already Integrated?\n\n**Yes — Clerk is the only auth system.** Migration is complete on branch `feat/clerk-only-auth` (PR #111). Evidence:\n\n- `auth.module.ts` — no `JwtModule`, no `PassportModule`, no `JwtStrategy`\n- `app.module.ts:99` — `ClerkAuthGuard` is the global `APP_GUARD`\n- `main.ts:111-124` — `clerkMiddleware` wired in production\n- `ClerkAuthProvider.tsx` — frontend fully on Clerk\n- `clerk-webhook.service.ts` — user provisioning via Clerk webhooks (`user.created`, `user.updated`, `user.deleted`, `organization.created/updated`)\n\nIf you were asking \"how hard would migration _from_ Clerk be\": it would be moderate effort. The main touch points are:\n- Replace `ClerkAuthGuard` (1 file, 95 lines) + `clerkMiddleware` in `main.ts`\n- Replace `ClerkAuthProvider` + `ClerkAuthBridge` in the designer frontend\n- Migrate `User.clerkId` column to the new provider's user ID\n- Replace the webhook sync service (`clerk-webhook.service.ts`)\n- User identity fields (publicMetadata) would need a new source\n\nBENCHMARK_REPORT_START\ntool_calls: 11\nconfidence: HIGH\ncomplete: Yes\nBENCHMARK_REPORT_END",
    "correctness": 0,
    "completeness": 1,
    "confidence": "HIGH",
    "pass": false
  },
  {
    "taskId": "R3",
    "method": "codesift-bm25",
    "repo": "tgm-survey",
    "durationMs": 137468,
    "inputTokens": 17,
    "outputTokens": 4723,
    "totalTokens": 4740,
    "cacheReadTokens": 632478,
    "cacheCreationTokens": 61491,
    "costUsd": 0.49123065,
    "numTurns": 11,
    "answer": "Here is the full authentication picture for the tgm-survey-platform codebase.\n\n---\n\n## 1. Auth Provider\n\n**Clerk** — fully integrated and the sole auth provider. Legacy JWT auth has been removed. The Clerk instance is `dashing-bass-97.clerk.accounts.dev`.\n\nKey files:\n- `apps/api/src/auth/clerk.guard.ts` — server-side guard\n- `apps/api/src/auth/clerk-user.helper.ts` — Clerk auth type + extractor\n- `apps/designer/src/api/client-utils.ts` — frontend token getter (`_getClerkToken`)\n\n---\n\n## 2. Login Flow (step by step)\n\n**Frontend (designer app)**\n\n1. Clerk SDK (`ClerkProvider`) wraps the React app — handles the hosted login UI, session lifecycle, and JWT issuance.\n2. `ClerkAuthProvider` (designer) sets a `_getClerkToken` getter at startup (`client-utils.ts:51`) using a deferred promise pattern to avoid 401 race conditions:\n   ```\n   let _getClerkToken: (() => Promise<string | null>) | null = null;\n   // set by ClerkAuthProvider at startup via session.getToken()\n   ```\n3. Every `apiFetch` call retrieves the current Clerk JWT and injects it as `Authorization: Bearer <token>`.\n\n**Backend (API)**\n\n4. **Strategy 1 — Production:** `clerkMiddleware` (Express middleware, registered in `main.ts`) intercepts the request, verifies the Clerk JWT with Clerk's JWKS, and populates `req.auth.userId`.\n\n5. **Strategy 2 — Dev fallback:** If `req.auth` is empty (clerkMiddleware skips Bearer tokens in dev mode), `ClerkAuthGuard.canActivate` manually base64-decodes the JWT payload and extracts the `sub` claim if it starts with `user_` (`clerk.guard.ts:47–62`):\n   ```typescript\n   const payload = JSON.parse(Buffer.from(parts[1], 'base64url').toString());\n   if (typeof payload.sub === 'string' && payload.sub.startsWith('user_')) {\n     clerkUserId = payload.sub;\n   }\n   ```\n   No signature verification in dev — relies on Clerk SDK to issue valid tokens.\n\n6. **DB user resolution** (`clerk.guard.ts:68–85`):\n   ```typescript\n   const dbUser = await this.prisma.user.findUnique({ where: { clerkId: clerkUserId } });\n   if (!dbUser || !dbUser.isActive) throw new UnauthorizedException();\n   ```\n\n7. **Session population:** Guard sets `req.user`:\n   ```typescript\n   req.user = { userId: dbUser.id, orgId: dbUser.organizationId,\n                clerkUserId, email, role, name };\n   ```\n\n**tRPC path** (designer CRUD): Same dual-strategy pattern lives in `apps/api/src/trpc/context.ts:createContext` (`context.ts:46–76`) — calls `extractClerkUserId(req)`, does DB lookup, and returns `TrpcContext.user`.\n\n---\n\n## 3. Guard Chain\n\nRegistered as global `APP_GUARD` in `apps/api/src/app.module.ts` in this order:\n\n| Order | Guard | File | Responsibility |\n|-------|-------|------|----------------|\n| 1 | `ClerkAuthGuard` | `auth/clerk.guard.ts` | Identity verification, `req.user` population. Skips routes with `@Public()` decorator. |\n| 2 | `RolesGuard` | `common/guards/roles.guard.ts` | Checks `@Roles()` metadata — enforces RBAC. |\n| 3 | `FeatureGateGuard` | _(feature-gate module)_ | Feature flag enforcement. |\n| 4 | `ThrottlerGuard` | _(NestJS throttler)_ | Rate limiting. |\n\n`app.module.ts:98–112`:\n```typescript\n{ provide: APP_GUARD, useClass: ClerkAuthGuard },\n{ provide: APP_GUARD, useClass: RolesGuard },\n{ provide: APP_GUARD, useClass: FeatureGateGuard },\n{ provide: APP_GUARD, useClass: ThrottlerGuard },\n```\n\n`OrgScopeGuard` (`common/guards/org-scope.guard.ts`) is **not** global — applied per-controller. It enforces multi-tenancy by matching `request.params.orgId` against `req.user.orgId`, with `super_admin` bypass.\n\n---\n\n## 4. Session/Token State Storage\n\n- **Stateless server** — no session store. Each request is independently validated via Clerk + DB lookup.\n- **Frontend:** Clerk SDK manages the JWT in its own secure browser storage (not `localStorage` or cookie you control). The token is retrieved on demand via `session.getToken()`.\n- **Per-request context:** `TrpcContext.user: JwtPayload | null` — in-memory only, not persisted.\n- **`JWT_SECRET`** still exists in `auth.module.ts` but only for preview token signing, not user authentication.\n- **No Redis session store** for user auth (Redis is used only for survey cache `SurveyCacheService`).\n\n---\n\n## 5. Is Clerk Already Integrated?\n\n**Yes — fully integrated.** The migration is complete on branch `feat/clerk-only-auth`. There is no legacy auth to migrate away from. The entire auth chain — frontend token issuance, Bearer header injection, server-side guard, tRPC context, DB user resolution — all run through Clerk.\n\n---\n\nBENCHMARK_REPORT_START\ntool_calls: 10\nconfidence: HIGH\ncomplete: Yes\nBENCHMARK_REPORT_END",
    "correctness": 0,
    "completeness": 1,
    "confidence": "HIGH",
    "pass": false
  }
]