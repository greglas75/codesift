# Code review — Task 9a–9c (79f06f0..83ea333)

## META

| Field | Value |
|-------|--------|
| Range | `5cdb537..83ea333` (commits 79f06f0, 5f2cfe8, 83ea333) |
| Intent | FEATURE — TS extractor: enum member constants, `is_async`, modifiers / accessor meta |
| Tier | TIER 2 — STANDARD (~230 LOC, 3 files) |
| Staleness | `83ea333..HEAD` modifies `typescript.ts` further; findings below re-checked at current `abstract_method_signature` branch on HEAD (`fca6482` line ~784): parity gap still present. |

## Tool Availability

| Tool / Index | Status | Used For |
|--------------|--------|----------|
| CodeSift index | UNAVAILABLE (not called this run) | — |
| analyze_hotspots | SKIPPED | tier pre-check |
| adversarial-review.sh | OK | 2× `--rotate` sequential passes |

## SCOPE FENCE

- `src/parser/extractors/typescript.ts`
- `tests/parser/typescript-extractor-gaps.test.ts`
- `tests/parser/typescript-extractor-declarations.test.ts`

## VERDICT

**PASS** — No MUST-FIX correctness issues in the reviewed range; targeted tests pass. Address RECOMMENDED items for API consistency (abstract signatures vs concrete methods, modifier vocabulary).

## DEPLOYMENT RISK

**LOW (0–1)** — Parser/index metadata only; no auth, API, DB, or payment paths.

## FINDINGS

### RECOMMENDED

**R-1** [RECOMMENDED] `abstract_method_signature` omits `is_async` and `accessor_kind` while `method_definition` sets them — e.g. `abstract async` or abstract accessors are misclassified for consumers filtering on those fields.  
**File:** `src/parser/extractors/typescript.ts` (`abstract_method_signature` case)  
**Confidence:** 78/100 — **Evidence:** diff + pass-2 cross-review; confirmed case block at ~784 still lacks `hasAsyncModifier` / `getAccessorKind` on HEAD.

**R-2** [RECOMMENDED] `accessor` auto-accessor keyword is not added to `meta.modifiers` (only `accessor_kind`), unlike `static` / `readonly` — consumers that only read `modifiers` miss it.  
**File:** `src/parser/extractors/typescript.ts` (`getModifiers` + field/method branches)  
**Confidence:** 62/100 — **Evidence:** cross-review pass 1; `MODIFIER_KEYWORD_TOKENS` excludes `accessor`.

### NIT

**R-3** [NIT] When `abstract` is force-appended to `abstract_method_signature`, order may not mirror source token order (`push` vs `unshift`).  
**File:** `src/parser/extractors/typescript.ts`  
**Confidence:** 58/100

### INFO / documentation

**R-4** [INFO] Enums now emit 1+N symbols (container + `constant` per member) — breaking for consumers assuming one symbol per enum; document or add stable `meta` for filtering.  
**Confidence:** 55/100

## DROPPED (confidence / duplicate)

- `enum_declaration` uses `return` vs `break`: fragile if post-switch logic is added — **52/100**, logged to backlog as hygiene.
- `hasAsyncModifier` only scans direct children — **55/100**, acceptable until failing fixtures exist.

## SKIPPED STEPS

- CodeSift Phase 0.5 pre-compute (optional; MCP not invoked in this Cursor run).
- `knowledge-curate.md` — optional include not loaded.

## VERIFICATION PASSED

```text
vitest run tests/parser/typescript-extractor-gaps.test.ts tests/parser/typescript-extractor-declarations.test.ts
→ 79 tests passed
```

## QUALITY WINS

1. Enum members surfaced as parented `constant` symbols improve search/navigation.
2. `is_async` on declarations, methods, and async arrows is consistently tested.
3. Modifiers + accessor kind covered by focused gap tests (L8/L9).

## TEST ANALYSIS

New/extended coverage in `typescript-extractor-gaps.test.ts` exercises enum members, async, modifiers, accessors, and TSX parity — aligns with production changes.
