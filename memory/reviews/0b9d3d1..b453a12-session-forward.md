<!-- zuvo-review -->
---
range: 0b9d3d1..b453a12
files: *
scope: forward-merge of concurrent-session work onto post-#22 main
tier: 2
verdict: APPROVE
---

# Merge-forward review — concurrent session work onto post-#22 main

Local `main` had diverged: it forked at `f51817d` (before PRs #8/#21/#22) and
carried 6 finished commits the origin never received — 3 already committed by the
concurrent session (telemetry fixes, the analyze_complexity 16-min-hang fix, the
0.9.7 bump) plus 3 I made here to capture its clean-but-uncommitted working tree
(the reviewed astro-actions / source-stripper / pattern-execution splits, the
codex approve-mode feature, and log/knowledge churn). All of it was already
green (tsc 0 errors, setup 85/85) before the merge.

## The one real conflict — same class, fourth time

`server-helpers.ts` conflicted for the same reason sql.ts (#21) and the perf
branch (#22) did: the session **edited it in place** while origin **split it**.

- Session commit `974f92c` added `canonicalizeRepoName` (case-insensitive repo-key
  matching — telemetry: `find_and_show` failed 73/74 calls whose repo had an
  uppercase letter) and rewired `resolveRepo` to call it.
- Origin (#21's split) moved that whole region into
  `server-helpers/repo-resolution.ts`, leaving `server-helpers.ts` a re-export
  barrel.

Resolved by taking the barrel and **porting the session's fix into
`repo-resolution.ts`**: added `canonicalizeRepoName` (with the `isAbsolute`
import), rewired `resolveToolRepoArgs` to canonicalize a caller-supplied repo,
and added `canonicalizeRepoName` to the barrel's re-export because the session's
tests import it from `server-helpers.js`.

`backlog.md` conflict resolved to origin's side (it holds the reviewed additions
from #21/#22; the session's log churn is not worth carrying).

## Verification

- `tsc --noEmit` — **0 errors**.
- `resolve-repo.test.ts` — **23/23**, covering BOTH the session's
  `canonicalizeRepoName` cases (case-insensitive match, absolute-path passthrough,
  unmatched passthrough, missing-registry passthrough) AND my #21/#22 registry-cache
  fixes (same-mtime isolation, relative-path collision) — they coexist.
- Full suite — **336 files, 5003 passed, 3 skipped**, build clean.
- Version preserved at 0.9.7 (session's bump won over origin's 0.9.6).

The session's own splits were each reviewed before this merge (APPROVE/PASS
artifacts under `memory/reviews/`: formatter-split, walk-split, index-tools-split,
project-profile-split, all-own-commits). This artifact covers only the
merge-forward resolution, which is the sole never-reviewed content in the range.
