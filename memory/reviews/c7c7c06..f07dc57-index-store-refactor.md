<!-- zuvo-review -->
range: c7c7c06f9bae9d37d68af785f64b33cfb509aac4..f07dc57933e3a6197a2a36c02dfe9443a2e097c0
files: src/storage/index-store.ts, src/storage/index-cache.ts, src/storage/index-migration.ts, src/storage/index-version.ts, src/storage/index-json-mutations.ts
adversarial: zuvo/proofs/refactor-index-store-adversarial.txt
verdict: PASS
<!-- /zuvo-review -->

# Review: `index-store` responsibility split

`src/storage/index-store.ts` remains the stable public facade while four focused modules now own cache state, backend/migration state, extractor-version checks, and JSON mutation batching. Production consumer import paths and public helper identities remain compatible. The facade fell from 859 to 400 lines, and no storage dependency cycle was introduced.

## Verification

- Scoped storage battery: 108/108 tests passed after a successful TypeScript build.
- Full repository battery through `rt`: 393 test files passed, 1 skipped; 5474 tests passed, 8 skipped.
- CodeSift: no storage cycles; maximum extracted-function complexity CC 10; no breaking, coupling, complexity, dead-code, bug-pattern, or test-gap failure.
- Test-quality audit: WARN because strict Q11 branch-completeness scoring floors three existing suites at Tier C; no critical test-quality gate failed.

## Regression evidence

New boundary tests first exposed acceptance of derived SQLite paths. The guard was then made backend-independent and covered `.db`, sidecars, and disguised `.db.json` paths. A follow-up red test caught an accidental change to legacy uppercase `.JSON` mapping; compatibility was restored before the final review.

## Adversarial disposition

The final full diff was reviewed in three untruncated chunks by four external provider families. No introduced critical issue remains. Critical reports about a SQLite cache TOCTOU window and broad version-validation error swallowing are present in the base revision and were moved without semantic change. Shallow nested cache copies, process-local JSON serialization, and path-keyed maps without eviction are also preserved design concerns; addressing them would require separate concurrency or data-integrity decisions outside this structural refactor.

The secret scan's two blocking-looking hits are false positives inside `ExtractorVersionMismatch` and `Promise<IndexOrStaleResult>` identifiers.
