<!-- zuvo-review -->
---
range: 5de5894..9ce04ba
files: "*"
scope: release diff plus symbol-tools refactor
tier: 3
date: 2026-08-10
verdict: APPROVE
---

# Review — symbol tools refactor and unreleased branch range

## META

| | |
|---|---|
| Range | `v0.14.0..9ce04ba` (34 commits) |
| Scope | 42 files, +5530/-1561 |
| Intent | REFACTOR + BUGFIX + TEST HARDENING |
| Worktree | `.worktrees/codex/refactor-symbol-tools` |
| Audit | CodeSift structural review + three cross-provider adversarial passes |

## VERDICT

**APPROVE.** No confirmed release blocker remains. Findings from the adversarial passes were
triaged against the implementation, fixed when reproducible, and covered by targeted tests.

## MATERIAL FIXES APPLIED DURING SHIP

- Preserved the `symbol-tools.ts` facade while splitting lookup, context, references, analysis, and
  internals into focused modules.
- Made stripped symbol IDs round-trip through single and batch lookups, kept collision refusal, and
  restored O(index + requested IDs) batch lookup behavior.
- Made reference scans asynchronous, pattern-consistent, failure-honest, Unicode-aware, and correct
  for identifiers containing `$`; explicit file patterns use the index-backed scanner.
- Hardened atomic NDJSON writes, corruption handling, registry/prune recovery, shared embedding cache
  persistence, embedding-provider output validation, and stale in-memory cache replacement.
- Separated telemetry cursors, coalesced in-process flushes, detected retro-log replacement, and
  required cursor persistence before reporting a successful flush.
- Hardened the agent reachability checker against malformed URLs, runtime option values, missing
  scripts, per-origin daemon health, and H19-prone pinned fallbacks.
- Restored test isolation for process environment variables and strengthened persistence,
  concurrency, shallow-history, and facade-contract tests.

## ADVERSARIAL REVIEW

The proof is `zuvo/proofs/ship-f046e2ed1e-adversarial.txt`. It contains multiple independent
`REVIEW BY:` records from Codex 5.3, Cursor Agent, Kimi, and Claude. The original 13-chunk release
diff review was followed by a 3-chunk fixes-only review and a final review of the last repair commit.

Confirmed findings were fixed. Lower-confidence residuals were either duplicate-upload risks in
best-effort anonymous telemetry, deliberate process-scoped behavior, or test suggestions that do not
demonstrate a production defect. No critical/high-confidence correctness or security defect remains.

## MUTATION TEST

The mutation test targeted the symbol-tools integration surface. Two meaningful mutants completed
within the skill timeout: the default-child-prefetch mutant initially survived, a regression test
was added, and the rerun killed both mutants (**2/2, 100%**). This is explicitly a time-limited,
small sample rather than a claim about repository-wide mutation coverage.

## VERIFICATION

- `rt npm run lint` — exit 0; Biome checked 981 files and `tsc --noEmit` passed.
- `rt npm test` — exit 0; **395 files passed, 1 skipped; 5511 tests passed, 8 skipped**.
- Symbol refactor target — **123/123** passed after the first fix round.
- Final changed-surface target — **131/131** passed.
- Mutation rerun — **2/2 mutants killed**.
- Secret scan — no high-confidence secret in the release diff; fixture strings were false positives.

## RELEASE FLOW

`[AUTO-DECISION] PR flow -> version + changelog deferred to main.`

No version bump, release changelog edit, or git tag is created on this branch. The reviewed branch is
ready to push and open as a non-draft pull request against `main`.
