# Code review: 5df5832..b247d02 (Task 10a/10b)

## Tool Availability

| Tool / Index       | Status                          | Used For                          |
|--------------------|---------------------------------|-----------------------------------|
| CodeSift MCP       | UNAVAILABLE (not invoked)       | Tier-1 review used git diff + vitest |
| Vitest             | OK                              | `typescript-extractor-gaps.test.ts` |

## Scope

- **Range:** `83ea333..b247d02` (commits `5df5832`, `b247d02`)
- **Files:** `src/parser/extractors/typescript.ts`, `tests/parser/typescript-extractor-gaps.test.ts`
- **Diff:** 2 files, +174 / −1

## Verdict

**WARN — merge OK; address completeness/metadata gaps as follow-ups.**

Anonymous default export synthesis, namespace/ambient handling, and `function_signature` / declare-const-without-initializer paths are covered by tests and behave plausibly. Cross-review surfaced **index completeness and symbol anchoring** issues that do not block CI but matter for symbol-graph consumers.

## Deployment risk

**LOW (0–1)** — parser/indexing behavior only; no auth, API, schema, or runtime contract changes.

## Findings (confidence ≥ 51)

### RECOMMENDED

**R-1** [RECOMMENDED] [CROSS:cursor-agent] Anonymous `default_export` uses early `return` after synthesis, so nested methods/functions inside the anonymous default body are **not** walked; named `export default function Foo()` still gets child processing.  
**File:** `src/parser/extractors/typescript.ts` (`export_statement` branch)  
**Confidence:** 88/100  
**Evidence:** Diff shows `symbols.push(sym); return;` before the usual `for (const child of node.namedChildren) walk(child, parentId, true)`.  
**Fix:** After pushing the synthetic symbol, recurse into the anonymous declaration body (or remove early return and merge with existing walk), matching named-default completeness.

**R-2** [RECOMMENDED] [CROSS:cursor-agent] Synthetic `default_export` calls `makeSymbol` with the **`export_statement`** node while `getSignature` / `returnsJSX` use the **inner** child — span/metadata may disagree with other symbols.  
**File:** `src/parser/extractors/typescript.ts` (same branch)  
**Confidence:** 82/100  
**Fix:** Anchor ranges on the declaration node or document dual-node semantics in `meta`.

**R-3** [RECOMMENDED] [CROSS:cursor-agent] Anonymous default detection only inspects direct `namedChildren` of a fixed type whitelist — patterns like parenthesized expressions or wrapper calls may miss synthesis.  
**Confidence:** 70/100  
**Fix:** Unwrap / broaden detection; add tests for patterns you care about.

**R-4** [RECOMMENDED] [CROSS:cursor-agent] `if (nsName)` drops **empty-string** module specifiers; edge `declare module ""` would skip namespace emission and body walk.  
**Confidence:** 74/100  
**Fix:** Use null-check vs truthiness, or emit placeholder + `meta`.

**R-5** [RECOMMENDED] [CROSS:cursor-agent] `function_signature` merged with declaration handling may emit **multiple symbols with the same name** for overload sequences in ambient contexts.  
**Confidence:** 67/100  
**Fix:** Dedup or overload-index `meta` in post-process.

### NIT

**R-6** [NIT] Namespace string name stripping is minimal (`replace(/^['"`]|['"`]$/g, "")`) vs escaped/complex literals — rare mismatch risk.  
**Confidence:** 55/100

**R-7** [NIT] `ambient_declaration` does not introduce a container symbol — hierarchy flattens vs nested namespace modeling.  
**Confidence:** 52/100

## Dropped / excluded

- Duplicate merge of R-1/R-2 root cause noted by reviewer; kept separate for tracking.
- Low-confidence items below 51 excluded from main list.

## Quality wins

1. Targeted Vitest coverage for L11 anonymous default, L2 namespace, L12 ambient/`declare module`, and export parity cases.
2. `declare const` without initializer now emits a searchable symbol — fills a real indexer gap.
3. `function_signature` included for ambient exports aligns AST shape with declaration extraction.

## Verification

- `npx vitest run tests/parser/typescript-extractor-gaps.test.ts` — **44/44 passed**

## Adversarial

- Pass 1: `adversarial-review.sh --rotate --mode code` → cursor-agent — 5 items (4× WARNING/INFO range).
- Pass 2: prior-findings prefix → cursor-agent — 5 new items (3× WARNING, 2× INFO).
