# Code review — `615bd67~1..713a4a8` (0.5.28 → 0.5.29 + ESM path fix)

## Tool Availability

| Tool / Index       | Status                         | Used For   |
|--------------------|--------------------------------|------------|
| CodeSift MCP       | UNAVAILABLE (not invoked)      | —          |
| Vitest             | OK (scoped `register-tools`)   | regression |

Full-suite run: partial failure in `sql-tools` tests (missing ephemeral repo index); unrelated to this diff. `tests/tools/register-tools.test.ts`: 71/71 passed.

## META

- Range: `615bd67~1..713a4a8` (commits `615bd67`, `e267296`, `713a4a8`)
- Reviewer environment: Cursor, sequential agents (no sub-agent dispatch)

## VERDICT

**PASS — merge approved.** The ESM `node:path` import correctly fixes `ReferenceError: require is not defined` in `hasJsxFilesShallow` under the package’s ESM graph; version bump aligns lockfile and manifest.

## DEPLOYMENT RISK

**LOW** (0–1): internal bootstrap / tooling registration only; no auth, schema, API, or payment changes.

## CHANGE SUMMARY

- `src/register-tools.ts`: replace runtime `require("node:path")` with top-level `import * as pathModule from "node:path"` and use `pathModule.join`; documents prior silent failure when `require` threw inside a swallowed catch path.
- `package.json` / `package-lock.json`: `0.5.27` → `0.5.29`.

## FINDINGS

### RECOMMENDED

- **R-1** [CROSS:cursor-agent] `detectFromPackageJson` still appears to absorb errors from `hasJsxFilesShallow` (comment notes this). Remaining I/O or logic failures could still disable React tool auto-load without visibility. **Confidence:** 72/100 — behavior predates this commit; fix does not worsen it.

### NIT / informational

- **R-2** [CROSS:cursor-agent] Top-level `node:path` import loads at module init vs previous lazy `require` inside the helper — acceptable for this Node MCP server; note if tests ever needed finer isolation.

### DROPPED / below threshold

- **[below-threshold]** [CROSS:codex-5.3] “Non-Node Astro edge runtime” startup failure — weak for `codesift-mcp`, which is a Node MCP server; **Confidence:** ~35/100.
- **[low-confidence]** [CROSS:codex-5.3] React detection via shallow `.tsx/.jsx` scan — pre-existing heuristic, not introduced here.

## QUALITY WINS

1. Clear regression narrative in-comment ties user-visible symptom (missing REACT_TOOLS in monorepo workspaces) to root cause.
2. Uses standard Node ESM builtin specifier (`node:path`) consistent with nearby `node:fs` typing.

## TEST ANALYSIS

- No test files modified in range.
- Targeted `register-tools` suite green (71 tests).
