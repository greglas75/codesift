<!-- zuvo-review -->
range: a97086d..83557a7
files: src/cli.ts, src/cli/commands.ts, src/cli/embed-child-marker.ts, src/cli/embed-child.ts, src/cli/hooks/pre-tool-use.ts, src/cli/hooks/shared.ts, src/cli/setup/mcp.ts, src/config.ts, src/lsp/lsp-tools.ts, src/parser/parser-pool.ts, src/register-tool-groups/core/search.ts, src/register-tool-groups/core/symbols.ts, src/register-tools.ts, src/register-tools/discovery.ts, src/retrieval/semantic-handlers.ts, src/search/semantic.ts, src/server.ts, src/storage/_shared.ts, src/storage/chunk-store.ts, src/storage/embedding-store.ts, src/storage/index-store.ts, src/storage/usage-tracker.ts, src/tools/complexity-tools.ts, src/tools/conversation-cache.ts, src/tools/conversation-index-tools.ts, src/tools/index-tools/folder-indexer.ts, src/tools/index-tools/parse.ts, src/tools/index-tools/registry.ts, src/tools/plan-turn/orchestrator.ts, src/tools/plan-turn/recommendations.ts, src/tools/symbol-tools.ts, src/utils/language-detect.ts
adversarial: zuvo/proofs/adv-a97086d-83557a7-chunk3.txt

# Code review — a97086d..83557a7

```
CODE REVIEW | TIER 3 (DEEP)
SCOPE:  53 files, +2647/-185 | INTENT: BUGFIX (19 commits, multi-session)
AUDIT:  TEAM (3 sub-agents) | Adversarial: codex-5.3, agy, cursor-agent, kimi, claude | RISK: MEDIUM
Risk signals: [x] >500L  [x] new prod files  [ ] DB  [ ] Auth  [ ] Money
SELF-REVIEW: yes (4 of 19 commits authored this session) -> adversarial run with --multi
```

`[CLASSIFIED] Diff type: mixed` — 32 production files, 18 test files, 3 other.

## Verdict

**APPROVE with fixes applied.** Five MUST-FIX defects found and fixed in `83557a7`;
each was reproduced before being fixed. Four high-severity claims were investigated
and **refuted** with evidence. Remaining confirmed findings are pre-existing debt in
earlier commits of this range and are backlogged, not blocking.

## Deployment risk

New production files (+1), >500 lines (+1) = **2 — MEDIUM**. Merge after review, run
full suite. No auth/payment/DB/API-contract surface touched.

## Tooling

| Tool | Result |
|---|---|
| `review_diff` | fail(score=0) — 81 "secrets" + 46 "removed exports", all triaged FALSE (below) |
| `changed_symbols` | 53 files, 96 added / 408 modified symbols |
| `diff_outline` | structural outline captured |
| `impact_analysis` | 20 affected symbols, 15 files in dependency graph, 10 affected tests |
| `scan_secrets` (high) | 1 real hit — `.env:1 openai sk-…`; untracked, `.gitignore:7`, never in history, not in push range |
| `search_patterns` empty-catch | 0 hits / 16,345 symbols |
| `find_references` | used on `saveEmbeddings`, `countBranches`, `estimateMaxNesting`, `detectProjectLanguagesSync` |
| stack-specific | not_required (plain TypeScript; no Nest/Next/Python framework surface) |

Adversarial coverage was **chunked**: the wrapper truncated 111,178 → 30,000 chars and
dropped 20 files, including every file changed this session. The diff was split into
5 chunks under the cap and each was run with `--multi`; proofs in `zuvo/proofs/`.

## MUST-FIX — applied in 83557a7

**R-1 CI was red and the local suite could not see it.** `tests/cli/hooks.test.ts:—`
bails out unless a codesift MCP server process is running. Dev machines have one; CI
does not, so all 28 deny-assertions failed there — GH Actions run `30302333468`
@`455cf87` (inside this range), 290 `permissionDecision` hits in the failure log. Every
"suite green" claim from this machine was environment-dependent. Fixed by pinning the
liveness gate in the test file and adding explicit coverage of the not-running branch.
Confidence 100/100 (CI run inspected directly).

**R-2 `isCodesiftServerRunning()` inverted its own contract.**
`src/cli/hooks/shared.ts:78` — docstring at `:52` states "Deliberately fails OPEN: any
error … means we do not block", but the catch returned `true`, which callers read as
"server up" and then deny the tool. An unreadable process table produced exactly the
outage the function exists to prevent. Now returns `false`. Confidence 100/100
(5 independent sources: behavior agent, CQ agent, 3 adversarial providers).

**R-3 Herestring swallowed the rest of the command.** `src/cli/hooks/pre-tool-use.ts:196`
— `<<<WORD` is not a heredoc; the scanner skipped the first `<`, read `<<WORD` as a
heredoc open, found no closing line, and consumed everything after it as body. So
`cat <<<EOF; rg "TODO" src/` silently under-blocked. Reproduced with a failing test
first. Authored this session. Confidence 100/100.

**R-4 Backtick substitution bypassed the check.** `src/cli/hooks/pre-tool-use.ts:283`
— the tick glued onto the command word (`` `grep `` != `grep`), so `` `grep -r foo .` ``
and ``X=`rg TODO src` `` slipped past. Backtick now splits like `$(...)`. Authored this
session. Confidence 95/100.

**R-5 `handleIndexRepo` never got the fix `handleIndex` got.** `src/cli/commands.ts:224`
— none of `CODESIFT_EMBED_OUT_OF_PROCESS` / `awaitPendingEmbeddings` / `runEmbeddingChild`,
so `codesift index-repo <url>` exits while the fire-and-forget embedding chain is in
flight and the repo ends up with partial or zero embeddings while reporting success.
Confidence 90/100.

## Refuted — investigated, evidence says no

- **Frozen-tool-list cross-session leak** (raised MUST-FIX by structure + CQ agents).
  `server.ts:334` attaches `oninitialized` to the module-level stdio server; `main()`
  branches on `CODESIFT_TRANSPORT === "http"` and the HTTP daemon serves from per-session
  instances created at `server.ts:214`, which never reach that handler. Not reachable.
- **`semantic-handlers.ts:110` "typo → runtime crash"** (CRITICAL, 2 providers).
  `ctx.filteredEmbeddings` is a real field; `semantic-handlers.ts:84` derives it from
  `chunkEmbeddings` via `filterEmbeddingsByFile`, so it is a subset with identical
  dimensions and the guard covers it.
- **`review_diff` 81 secrets** — the `tru-2` rule matching truncated identifiers
  (`extr***bols`, `CODE***IST=` is an env var NAME). Zero real secrets in the diff.
- **`review_diff` 46 removed exports** — fixture source strings written by
  `tests/integration/tools.test.ts`, not an API surface.
- **coupling gap on `src/tools/plan-turn-tools.ts`** — a 17-line facade re-exporting
  `plan-turn/*`; the changed logic flows through it unmodified.
- **`plan-turn/orchestrator.ts:224` `&&` composition** — both operands derive from
  `CORE_TOOL_NAMES`, so the conjunction is idempotent.

## Backlogged — confirmed, pre-existing, not blocking

- `src/search/semantic.ts:11` — `EMBEDDING_TIMEOUT_MS` reads `process.env` at import
  time, bypassing `config.ts` (the repo's own documented gotcha), untestable.
- `src/storage/chunk-store.ts:142` — temp name is timestamp-only; `atomicWriteFile`
  added pid+random for this exact collision and the streamed rewrite dropped it.
- `src/storage/chunk-store.ts:154` — a stream error while awaiting `drain` never
  resolves; the save can hang and the stream is not destroyed on the error path.
- `src/parser/parser-pool.ts:104` — `worker.unref()`; the justifying comment claims
  pending awaits keep the loop alive, which is not how Node liveness works.
- `src/register-tool-groups/core/symbols.ts:100` — batch `get_symbols` can push the
  same resolved canonical id twice (`foundIds` never updated inside the loop).
- `src/tools/symbol-tools.ts:1061` — `findUnusedImports` cyclomatic complexity 31
  (not in this diff).
- `src/cli/hooks/pre-tool-use.ts` — substitution nested inside double quotes
  (`echo "$(rg x)"`) is not inspected; documented in-code as an accepted limit.

## Test analysis

18 test files changed; 7 new. All judged non-vacuous by the CQ auditor — each asserts
observable state (filesystem, return values, write counts) rather than mock calls. The
one real gap it named (no coverage of `isCodesiftServerRunning()` returning false) is
what broke CI and is now covered.

Full suite after fixes: **357 files, 5133 passed, 3 skipped.**
