# Code review: `f570c4c^..fc4866b` (8ab349d → 22213ec → fc4866b)

## Tool Availability

| Tool / Index       | Status                         | Used For        |
|--------------------|--------------------------------|-----------------|
| CodeSift MCP       | UNAVAILABLE (not invoked)      | —               |
| Vitest             | OK (7/7 tests in scope)        | changed tests   |
| adversarial-review | OK (2 passes, codex + claude) | cross-provider  |

## META + SCOPE

- **Commits:** `8ab349d` (spec + plan docs), `22213ec` (`CodeSymbol.implements`), `fc4866b` (`makeSymbol` opts).
- **Mixed:** production (`src/types.ts`, `src/parser/extractors/_shared.ts`), tests, large spec markdown under `docs/specs/`.

## VERDICT

**PASS — merge acceptable** for Tasks 1–2 of the staged plan: schema + `makeSymbol` plumbing matches existing `extends` behavior; targeted tests pass.

## Deployment risk

**LOW** — additive optional field; no auth/DB/API/payment changes.

## Severity summary

| Tier        | Count |
|-------------|------:|
| MUST-FIX    | 0     |
| RECOMMENDED | 2     |
| NIT / INFO  | 3     |

## Change summary

- `CodeSymbol` gains optional `implements?: string[]` (TypeScript interface names).
- `makeSymbol` accepts `implements` in opts and assigns when non-empty (mirrors `extends`).
- New unit/type tests cover plumbing and schema typing.
- Two large spec/plan markdown files document P0+P1 gaps and TDD tasks.

## SKIPPED STEPS

- CodeSift pre-compute (not used).
- Multi-agent CQ/Behavior dispatch (TIER 0 — handled inline).

## Verification

```text
npx vitest run tests/parser/_shared.test.ts tests/storage/code-symbol-schema.test.ts
→ 7 passed
```

## FINDINGS

### RECOMMENDED

**R-1** — `[CROSS:codex-5.3]` **Reference sharing for `implements` (and existing `extends`)**  
`sym.implements = opts.implements` shares the caller array; same pattern as `extends`. Low practical risk for synchronous extractor code; consider spreading `[...opts.implements]` if callers ever reuse buffers.  
Confidence: **72/100**

**R-2** — `[CROSS:claude]` **Downstream graph / tooling**  
At `fc4866b`, `GraphEdge.kind` already includes `"implements"`, but nothing reads `sym.implements` to build edges; `extends` has limited consumers (e.g. Pydantic path). Acceptable for tasks 1–2; track for later tasks when extractor/index pipeline emits `implements`.  
Confidence: **62/100**

### NIT / INFO (below merge gate)

**R-3** — `[CROSS:codex-5.3]` Normalization of interface names (trim/dedupe/NFC) — not done for `extends` either; defer unless product requirement. Confidence: **38/100** → excluded from merge discussion.

**R-4** — `[CROSS:codex-5.3]` Kind-gating `implements` to class/interface-only — optional invariant; type-level field is permissive by design for phased rollout. Confidence: **42/100**

**R-5** — `[CROSS:claude]` Extractor version bump concern — **invalid at reviewed tip:** `EXTRACTOR_VERSIONS.typescript` was **2.1.0** at `fc4866b`; adversarial confused with later `HEAD`. N/A for this range.

## QUALITY WINS

1. Symmetry with `extends` in `_shared.ts` keeps extractor API predictable.
2. Tests explicitly cover empty array vs omitted vs interaction with `extends`.
3. Large written spec/plan improves traceability for the remaining ~17 tasks.

## CQ self-eval (production files)

### `_shared.ts` (delta only)

CQ1–CQ29: **29/29 PASS** for this diff — no new control flow, secrets, org-boundary queries, or error swallowing; change is optional-field assignment parallel to `extends`.

### `types.ts` (delta only)

CQ1–CQ29: **29/29 PASS** — additive optional interface member only.

## Q1–Q19 (test files)

### `tests/parser/_shared.test.ts`

Q1–Q19: **19/19 PASS** — Vitest patterns, meaningful assertions, parser setup in `beforeAll`, no flaky sleeps; covers propagation and omission behavior.

### `tests/storage/code-symbol-schema.test.ts`

Q1–Q19: **19/19 PASS** — compile-time type tests via `expectTypeOf`; minimal but appropriate for schema validation.

## Adversarial summary

- **Pass 1:** `codex-5.3` — reference sharing, normalization, kind semantics.
- **Pass 2:** `claude` — graph wiring, version bump (fact-checked **false** at tip), e2e extractor coverage (fair forward-looking note for later tasks).

---

*Generated by zuvo:review protocol (Cursor), scope validated `git log f570c4c..fc4866b`.*
