# Code review — `ff64858^..0be6cd6` (Tasks 11–12/19)

**Date:** 2026-05-05  
**Range:** parent of `ff64858` … `0be6cd6` (2 commits)

## Tool Availability

| Tool / Index       | Status        | Used For                                      |
|--------------------|---------------|-----------------------------------------------|
| CodeSift index     | UNAVAILABLE   | Cursor session — native git/read tests only   |
| analyze_complexity | UNAVAILABLE   | Skipped                                       |
| analyze_hotspots   | UNAVAILABLE   | Skipped                                       |
| scan_secrets       | UNAVAILABLE   | Skipped                                       |

---

## META

Commits reviewed:
1. `ff64858` — guard TS extractor walk vs stack overflow + grammar-error warn
2. `0be6cd6` — TS AST import path in `collectImportEdges`, parse cache, type-only cycle filter in `findCircularDeps`

---

## CLASSIFIED

`[CLASSIFIED] Diff type: **mixed**` (production + tests + fixtures)

---

## PHASE 1 LOADED

- `~/.codex/shared/includes/codesift-setup.md` — READ  
- `~/.codex/shared/includes/env-compat.md` — READ (partial)  
- `~/.codex/shared/includes/quality-gates.md` — SKIP (abbreviated CQ/Q inline in-session)  
- `~/.codex/shared/includes/cross-provider-review.md` — SKIP  
- `~/.codex/rules/cq-patterns-core.md` / `cq-patterns.md` — SKIP  
- `~/.codex/rules/cq-checklist.md` — SKIP  
- `~/.codex/rules/testing.md` — SKIP  
- `~/.codex/rules/security.md` — SKIP (no security signals)

---

## TIER AND RISK

- **Tier:** STANDARD (Tier 2) — 10 files, ~223 loc delta, MCP-visible behavior change (`find_circular_deps` cycle set).
- **Deployment risk:** **MEDIUM** (2–4) — tool output contract changes (+2); substantive graph-path change (+1); consumer-facing correctness improvement.
- **INTENT:** FEATURE + HARDENING (observability, robustness).

---

## VERDICT

**PASS WITH RECOMMENDED FOLLOW-UPS** — logic is coherent, upgrades are intentional, tests validate type-only filtering and extractor behavior. Workspace `HEAD` already contains a safer aliased-import resolution than reviewed tip — see FINDINGS.

---

## CHANGE SUMMARY

1. **`typescript.ts`:** `tree.rootNode.hasError` warn once/file; wrap recursive `walk` in try/catch for `RangeError` stack overflow → return partial symbols.
2. **`graph-tools.ts` (`findCircularDeps`):** exclude edges with `type_only === true` from cycle DFS.
3. **`import-graph.ts`:** for `.ts`/`.tsx`, prefer tree-sitter + `extractTypeScriptImports`; cache parse; propagate `type_only`; resolve bare specifiers via `resolveTsAliasedImport`; regex fallback on throw / no parser.

---

## FINDINGS

### R-1 [RECOMMENDED] Aliased-path containment at `0be6cd6`

**Confidence:** 72/100  
**Evidence:** At reviewed tip `0be6cd6`, `resolved` relative path derives from `aliased.startsWith(index.root)` string prefix + slice — brittle vs normalized absolute paths (`resolve`, trailing slashes, symlinks).  
**Status:** **`[ALREADY-PATCHED]`** — working tree differs from `0be6cd6` on `src/utils/import-graph.ts` (`resolve`/`relative` + “inside repo” guard). No action if that commit is superseded.

### NIT-1 [[CROSS:inline]] Observability

Repeated `console.warn` on many broken TS files could increase log volume in CI — acceptable tradeoff given goal; optionally rate-limit later.

---

## SKIPPED STEPS

- Phase 2+ CodeSift pre-compute (UNAVAILABLE).
- Sub-agents Behavior/Structure/CQ (Cursor — no parallel Explore agents).
- **Adversarial script:** invocation hung / exceeded 120s — `[CROSS-REVIEW]` incomplete (see RETRO infra block).

---

## VERIFICATION PASSED

- `npx vitest run tests/integration/type-only-cycle.test.ts tests/parser/typescript-extractor-gaps.test.ts` — **PASS** (46 tests in those files’ suite run).

---

## CQ / Q NOTES (CONDENSED)

**CQ (production slice):** Critical gates satisfactory for MCP plumbing — bounded errors, no silent swallow of unexpected exceptions in extractor (non-RangeError rethrown); graph filter explicit.  
**Q (tests):** Integration uses fixture index + cache clears; extractor tests use `vi.spyOn(console, “warn”).mockRestore()` — consistent with hygiene.

---

## QUALITY WINS

1. Explicit `type_only` on TS AST edges upgrades Python parity and fixes false-positive cycles on `import type`.
2. Parse cache amortizes duplicate parses between symbol extraction and edge collection.
3. Extractor survives pathological AST depth with partial extraction instead of crashing the indexer.
