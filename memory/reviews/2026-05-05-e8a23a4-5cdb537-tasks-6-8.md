# Code review — `e8a23a4^..5cdb537` (tasks 6–8 / 19)

## Tool Availability

| Tool / Index       | Status                         | Used For              |
|--------------------|--------------------------------|-----------------------|
| CodeSift MCP       | UNAVAILABLE (this Cursor run) | Pre-compute skipped   |
| Vitest             | OK                             | Regression on scope   |
| adversarial-review | OK (gemini + codex-5.3)        | Cross-provider passes |

---

## META

**Scope:** `e8a23a4^..5cdb537` — commits `e8a23a4`, `9fd3799`, `5cdb537`  
**Validated:** `git log` non-empty; `git diff --shortstat`: 5 files changed, 264 insertions(+), 23 deletions(-)

---

## [CLASSIFIED] Diff type: **mixed** (production + tests)

---

## PHASE 1 — LOADED

| Include | Status |
|---------|--------|
| `codesift-setup.md` | READ |
| `env-compat.md` | READ (partial) |
| `quality-gates.md` | READ — CQ1–CQ29 + Q1–Q19 sections |
| `cq-checklist.md` | READ — gates table + High-Risk (PURE → cq-patterns-core path applicable) |
| `cq-patterns-core.md` | SKIP (inline CQ via checklist reference) |
| `cross-provider-review.md` | SKIP — script invoked directly |
| `testing.md` | SKIP — condensed Q gates from quality-gates |
| `security.md` | SKIP — no auth/secret signals in diff |
| `knowledge-prime.md` | MISSING → degraded skip |
| `knowledge-curate.md` | DEFERRED → not loaded |

---

```
===============================================================
CODE REVIEW | TIER 2 (STANDARD)
SCOPE:  5 files, +264/-23 lines | INTENT: FEATURE (extractor + MCP helper)
AUDIT:  SOLO | Adversarial: gemini, codex-5.3 (sequential --rotate) | RISK: MEDIUM
Risk signals: [ ] API  [ ] DB  [ ] Auth  [ ] Money  [ ] 500+L  [x] new prod module (_helpers.ts)
===============================================================
```

**Deployment risk score:** ~3 (MEDIUM) — MCP-visible error copy (`staleToMcpError`) + new helper module + indexer metadata (`extends`/`implements`) affecting downstream consumers; run full CI before merge.

---

## VERDICT

**PASS with WARN** — Correctness fixes (duplicate `:` in signatures, ES6 `extends Foo` heritage, generics in signatures) are well covered by new `typescript-extractor-gaps.test.ts` and updated declaration tests. Address duplicate `getClassHeritage` calls per class before treating CQ14 commentary as satisfied.

---

## CHANGE SUMMARY

1. **`staleToMcpError`** — Pure MCP `{ isError: true, content }` mapper from stale-index fields; unit-tested.
2. **Class heritage** — `extends` / `implements` arrays on class symbols via `extractHeritageNames` + `getClassHeritage`; React class-component detection reuses heritage list (fixes dropped `identifier` extends).
3. **Signatures** — Prepends `type_parameters`; fixes double-colon bug by slicing `return_type` without extra `": "`.

---

## CQ evaluation

### `src/parser/extractors/typescript.ts` (~691 LOC touched region)

Abbrev. gates (extractor = mostly boundary-N/A): CQ3–CQ7,CQ9,CQ16,CQ19–CQ21,CQ23–CQ24,CQ26–CQ28 = **N/A** (no HTTP/DB/money/logger/cache). Active highlights:

| Gate | Score | Note |
|------|-------|------|
| CQ1 | 1 | No loose equality introduced |
| CQ2 | 1 | Helpers typed; no `any` |
| CQ8 | 1 | No new empty catches |
| CQ10 | 1 | Null-safe child walks |
| CQ11 | 1 | Helpers remain small |
| CQ12 | 1 | React bases remain named set |
| CQ13 | 1 | No dead exports |
| CQ14 | 1 | Heritage logic centralized vs prior duplicate walk inside old `isReactClassComponent` — **but call site still invokes `getClassHeritage` twice** (see R-1); comment overclaims |
| CQ22–CQ29 | N/A or 1 | No timers/subscriptions |

**CQ rollup (active only):** PASS on critical gates; CQ14 commentary misleading → fix R-1 or adjust comment.

### `src/tools/_helpers.ts`

Pure helper; CQ3–CQ8 boundary gates **N/A**; CQ5 **1** (error text is operational metadata only — versions/reason codes).

---

## Q evaluation (tests)

### `tests/parser/typescript-extractor-gaps.test.ts` (NEW)

`Q EVAL: typescript-extractor-gaps.test.ts | Q1=1 Q2=1 Q3=N/A Q4=1 Q5=1 Q6=1 Q7=1 Q8=0 Q9=1 Q10=1 Q11=1 Q12=0 Q13=1 Q14=1 Q15=1 Q16=1 Q17=1 Q18=1 Q19=1 | Score: 16/17 applicable -> PASS | Critical: Q7=1 Q11=1 Q13=1 Q15=1 Q17=1 -> PASS`

*(Q8/Q12: no explicit negative/null heritage cases — optional hardening.)*

### `tests/parser/typescript-extractor-declarations.test.ts` (delta)

Expectations updated for signature colon fix — **PASS** on critical gates.

### `tests/tools/_helpers.test.ts` (NEW)

`Q EVAL: _helpers.test.ts | Q1=1 Q2=1 Q3=N/A Q4=1 Q5=1 Q6=1 Q7=1 Q8=0 Q9=N/A Q10=1 Q11=1 Q12=0 Q13=1 Q14=1 Q15=1 Q16=1 Q17=1 Q18=1 Q19=1 | PASS`

---

## Adversarial (sequential `--rotate`)

**Pass 1 — gemini:** ECS-style `*.Component` false-positive for React class detection; `getSignature` gap for unparenthesized arrows; mixin `call_expression` heritage; whitespace in member_expression text.

**Pass 2 — codex-5.3:** `return_type` assumes `type_annotation` shape; **duplicate `getClassHeritage` pass** (`isReactClassComponent` then extract path); silent drop on unknown heritage nodes; `staleToMcpError` string injection INFO.

---

## FINDINGS (merged, deduped)

### MUST-FIX

*(none)*

### RECOMMENDED

**R-1** — Duplicate heritage traversal per class symbol  
**File:** `src/parser/extractors/typescript.ts` (~760–764)  
**Confidence:** 88/100 — Evidence: `isReactClassComponent(node)` calls `getClassHeritage`; next line calls `getClassHeritage(node)` again. Contradicts header comment at ~160.  
**Fix:** Compute `heritage` once; implement `isReactClassComponentFromExtends(extendsList: string[])` or pass cached `{ extends }`.

**R-2** — React class heuristic matches any `*.Component` / bare `Component`  
**File:** `typescript.ts` (`isReactClassComponent`)  
**Confidence:** 74/100 — **Pre-existing behavior** (member_expression property check already matched `Component`); still risks ECS-style names.  
**Fix:** Optionally tighten to `React.Component` / `React.PureComponent` / bare bases in `REACT_COMPONENT_BASES`.

**R-3** — `getSignature` / heritage edge coverage  
**Confidence:** 55–65/100 — Assertions/predicate returns; unparenthesized arrow params; mixin `extends foo()` — treat as backlog unless product guarantees TS grammar subset.

### NIT

**R-4** — Over-strong CQ14 comment (“NO duplicate AST walk”) while call site duplicates traversal — documentation hygiene.

**R-5** — `staleToMcpError` could sanitize control chars / cap length if index metadata ever hostile (**INFO** severity from codex).

---

## DROPPED / BELOW THRESHOLD

- Whitespace normalization in `extends` strings — INFO, merge OK without blocking.

---

## SKIPPED STEPS

- CodeSift `analyze_hotspots` / `impact_analysis` — MCP unavailable this run.
- `reviewed/<hash>` git tags — applied locally per protocol (see gate).

---

## VERIFICATION PASSED

```text
npx vitest run tests/parser/typescript-extractor-gaps.test.ts \
  tests/parser/typescript-extractor-declarations.test.ts tests/tools/_helpers.test.ts
→ 3 files, 82 tests passed
```

---

## QUALITY WINS

1. Regression tests lock ES6 `extends Foo`, generics-in-signature, and corrected return-type slicing.
2. `extends`/`implements` metadata aligns with existing `CodeSymbol` schema (`types.ts`).
3. `staleToMcpError` isolates MCP envelope shaping for reuse.

---

## TEST ANALYSIS

New gap suite exercises TS + TSX parsers (`beforeAll` guard). Declaration tests prove signature normalization. No new tests required for merge; optional negatives (null heritage, mixin extends) would raise Q8/Q12.

---

## QUESTIONS FOR AUTHOR

1. Should React class detection remain permissive (`Any.Component`) for DX, or tighten to reduce ECS-style false positives?
2. Prefer single heritage computation at class emission (R-1) in a follow-up commit on this branch?
